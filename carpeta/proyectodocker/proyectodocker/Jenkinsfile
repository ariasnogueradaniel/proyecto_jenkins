pipeline {
    agent any

    stages {
        stage('Docker Compose Build') {
            steps {
                bat """
                    cd carpeta\\proyectodocker\\proyectodocker
                    docker-compose -f docker-compose.yml build
                """
            }
        }
    }
}
