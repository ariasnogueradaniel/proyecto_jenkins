pipeline {
    agent any

    stages {
        stage('Construir Imagen de Desarrollo') {
            steps {
                script {
                    bat "docker-compose build"
                }
            }
        }
    }
}
